# CIDRKnight
CIDRKnight is a Python-based network reconnaissance tool that performs ICMP host discovery and multithreaded TCP port scanning on CIDR subnets, generating structured JSON and Markdown reports.
